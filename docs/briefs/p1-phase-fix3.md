TASK (StarBaron PHASE 1 whole-phase audit FAIL round 3 — FIX ALL 4 FINDINGS, no broadening; this closes Phase 1):

ALLOWED FILES (ONLY): src/sim/world/galaxy.ts, src/sim/world/system.ts, src/sim/world/body.ts, src/sim/world/catalogue.ts, src/sim/world/api.ts, src/sim/world/reconstruct.ts, src/sim/world/trust.ts (DELETE it — see finding 2), supabase/migrations/0013_world_schema.sql, supabase/tests/07_world_schema.sql, tests/galaxy-model.test.ts, tests/system-model.test.ts, tests/body-model.test.ts, tests/catalogue.test.ts, tests/api.test.ts, tests/reconstruct.test.ts. Do NOT touch anything else.

RESTRICTIONS: purity preserved; no `any`; strict TS; deterministic; no literal banned comment tokens (as before); SQL write-only.

FINDINGS (fix exactly these):

1. [T01/T03/T04/T06 — supabase/migrations/0013_world_schema.sql:174] `UNIQUE (system_id, ordinal)` is too strict: type is part of identity, so `body:<g>|<s>|planet|0` and `body:<g>|<s>|moon|0` must BOTH be allowed in one system. Fix: forward-migrate to `UNIQUE (system_id, type, ordinal)` (drop the old constraint), update the migration commentary, and add persistence coverage for same ordinal across distinct types (e.g. planet|0 + moon|0 in one system both persist; duplicate same-type same-ordinal still raises).

2. [T04/T05/T07 — src/sim/world/trust.ts + factories] The exported CATALOGUE_TRUST symbol is forgeable — any module can mint a trust object and elevate realData. Fix: DELETE trust.ts and REMOVE the `trust` parameter from all three public factories (galaxy/system/body) entirely — factories accept NO realData/provenance input at all and ALWAYS produce procedural records (realData false, provenance 'procedural'). Catalogue labelling moves to catalogue.ts as a post-construction spread at its single mapping boundary: `{ ...builtRecord, realData: true, provenance }` — one labelled place, no public elevation path. Update catalogue.ts accordingly (it no longer passes trust args; it overrides after building). Negative tests: it must be IMPOSSIBLE to construct a realData:true record via the public factories (no input path — assert the factory input type no longer accepts it and runtime default is always false); a direct object spread outside catalogue is out of scope (plain-data reality — the SQL CHECKs + validateCatalogue are the enforcement net, document this in catalogue.ts JSDoc).

3. [T05 — src/sim/world/catalogue.ts:335-402] validateCatalogue never verifies SystemRecord.bodyIds registries — a mapping with correct bodies but missing/swapped system registries validates OK. Fix: for every system, require an ordered, duplicate-free bodyIds registry EXACTLY matching the set of mapped child body ids (each body whose parent is that system must appear; no extra ids; no dupes). Add negative-path tests (registry missing an entry; registry has a foreign id; registry has a duplicate).

4. [T04/T06 — supabase/migrations/0013_world_schema.sql:156-181] SQL permits non-star bodies with period=0 (and defaults newly inserted planets to zero), while buildBodyRecord rejects non-star period <= 0. Fix: schema enforces star zero-orbit vs non-star positive period — e.g. `CHECK (type = 'star' OR period > 0)` and `CHECK (type = 'star' OR semi_major_axis > 0)`, plus `CHECK (type <> 'star' OR (semi_major_axis = 0 AND period = 0))`-style consistency (design the exact CHECK set so stars are all-zero and non-stars have positive period + semiMajorAxis). Update SQL fixture inserts with valid non-star orbit values (existing planet/moon/asteroid fixtures must set period/semi_major_axis > 0). Keep the model's rule as the documented source.

VERIFY (focused per-file runs only): `npx tsc -b` exit 0; `npx vitest run tests/galaxy-model.test.ts tests/system-model.test.ts tests/body-model.test.ts tests/catalogue.test.ts tests/api.test.ts tests/reconstruct.test.ts` all pass (per-file counts); SQL static read-back only.

REPORT: per-finding changed lines + which test covers which finding + any signature changes (removal of trust param is intended).
