TASK (StarBaron VERTICAL SLICE — Path B: the FIRST PLAYABLE GAME with the new sim wired in). One coherent UI-integration pass over the React shell. The sim brain is DONE (economy/fleets/combat, ~3,100 tests) — this wires it into the playable game.

GOAL: the player can, in the live game: (1) see REAL economy numbers ticking (credits via the ledger + income formula, population via the population model, structure effects), (2) BUILD structures with the real cost/cap framework, (3) BUILD SHIPS in the shipyard (real locked costs), (4) see their FLEET, (5) LAUNCH AN ATTACK on a target planet (real launch cost via the estimator), (6) watch it TRAVEL (movement/positioning), (7) see the battle RESOLVE with a combat report (resolution/casualties/report). All state from the NEW pure sim modules — nothing hand-rolled.

ALLOWED FILES: src/ui/useGameState.ts, src/ui/PlanetView.tsx, src/ui/components/* (new components allowed under src/ui/components/), src/ui/save.ts (only if the save shape needs the new state — keep migrations), src/App.tsx if needed, src/ui/index.css + App.css (styling). READ but do NOT modify: src/sim/** (pure, locked), tests/**.

RESTRICTIONS: TypeScript strict (repo tsconfig); no `any`; NO wall-clock in the SIM layers (all time inputs come from the tick clock in useGameState); React 19 + Vite; the established UI taste: DARK theme (deep navy-black #0b0e14-style), restrained professional, ORANGE accent only (vivid-not-neon, dark text on bright orange for primary buttons), data-dense + snug (fits the viewport, mobile-first — phone is the primary platform), no emoji watermarks, no fake numbers. The existing components' visual language (ResourceBar, StructureGrid, BuildMenu) is the baseline — match it, extend it.

WIRE-UP SPEC (read each sim module's exports and USE THEM — do not re-implement formulas):

1. ECONOMY TICK (useGameState): the game already has an interval tick (line ~481). Replace hand-rolled income/population math with the LOCKED modules: read src/sim/core/transactions.ts (credit delta), src/sim/core/economy.ts or src/sim/player/accrual.ts (income per second formula — READ and mirror the exact accrual call), src/sim/core/population-model.ts (population growth), src/sim/structures/effects.ts (structure effects + nextBuildCost + level cap — the game's BuildMenu already uses nextBuildCost: VERIFY it uses the locked one and the LOCKED canBuild/requirements ladder from src/sim/structures/framework.ts). Where the shell already calls locked functions, keep them; where it hand-rolls (income, population per sec, structure effects), swap to the locked exports. ALLOY support: the ledger tracks alloys — show the Alloy balance.

2. SHIPYARD + FLEET (new components: ShipyardPanel.tsx + FleetPanel.tsx): build ships via src/sim/fleet/shipyard.ts (locked build eligibility + costs: READ its exports — startBuild/elapsed/buildEligibility etc. and mirror the exact call shape); each ship class from src/sim/fleet/ships.ts (SHIP_CLASSES — name, cost credits/alloys, speed, combat, scouting, cargo); the fleet list shows composition (src/sim/fleet/fleet.ts — FleetComposition, fleetCompositionSize). Ships cost real credits+alloys and take build time (buildTimeSec — the tick completes them).

3. ATTACK FLOW (new component: AttackPanel.tsx): pick a target — show a small list of OTHER planets (use the existing PLANETS catalogue: the eligible non-home worlds — reuse the eligibleHomeWorlds/selectedPlanet machinery); launch via src/sim/combat/attack-orders.ts (attackLaunchCost via estimator — READ its signature: needs fleetSize + distancePc; distancePc: use a fixed deterministic distance for the slice (e.g. 10 pc — document) or the existing world distance if available); the ATTACK TRAVELS: store launchAt/arrivalAt, and the HUD/panel shows 'Attack in transit · ETA 1h 12m' using src/sim/fleet/positioning.ts or movement.ts (arrivalTime); on arrival tick → resolve via src/sim/combat/resolution.ts (resolveBattle — needs troops + shipyardTier + turretLevels + population: use the DEFENDER planet's data — for the slice the defender is a NPC planet with draft stats (turrets 2, population 5,000 — DOCUMENT as placeholder until P8+ adds real players) → outcome → apply src/sim/combat/casualties.ts (applyCasualties) → show the COMBAT REPORT (src/sim/combat/combat-reports.ts — buildCombatReport + reportText) in the panel; a VICTORY flips the planet to YOUR ownership (simple local state change for the slice — the conquestTransfer machinery is P12 wiring; document). Keep it deterministic and replayable.

4. HUD: extend ResourceBar with the fleet count + alloy balance + (when an attack is in transit) the ETA line.

5. SAVE: the existing save.ts must persist the new state (fleet, credits, alloys, attack-in-transit) — extend the SaveGameV3 shape with a v4 migration (follow the existing migration pattern in save.ts).

VERIFY: `npx tsc -b` exit 0; `npx vitest run` on the EXISTING test suite (tests/save.test.ts tests/planetview*.test.tsx tests/player-save.test.ts — the ones touching the shell) — all pass (report counts). DO NOT run the full suite. Then `npm run dev` boots clean (report the localhost URL).

REPORT: per-wiring-point changed files + what the player can now do end-to-end + any locked-module call-shape adjustments you had to make (with the exact function names used).
