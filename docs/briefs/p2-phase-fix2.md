TASK (StarBaron PHASE 2 whole-phase audit FAIL round 2 — FIX ALL 5 FINDINGS, no broadening; this closes Phase 2):

ALLOWED FILES (ONLY): src/sim/player/ownership.ts, src/sim/player/protection.ts, src/sim/player/transfer.ts, src/sim/player/onboarding.ts, src/sim/player/player.ts, src/sim/player/id.ts (MOVE — see finding 4), NEW src/boundary/id.ts, src/sim/player/claim.ts (if needed), supabase/migrations/0016_ownership_canonical.sql (append to it — it is THIS round's fix vehicle; ALTER/CREATE OR REPLACE only), supabase/tests/10_home_claim_atomicity.sql, and the affected test files (tests/ownership.test.ts, protection.test.ts, transfer.test.ts, onboarding.test.ts, claim.test.ts, player-save.test.ts if it imports generatePlayerId). src/ui/PlanetView.tsx + src/ui/useGameState.ts + src/sim/player/save.ts ONLY for compile-required import updates. Do NOT touch anything else.

RESTRICTIONS: purity preserved; no `any`; strict TS; no banned comment tokens; SQL write-only + idempotent; migration 0016 may be extended with additional ALTER/CREATE OR REPLACE statements (append at the end with section comments) — never edit 0014/0015/0005.

FINDINGS (fix exactly these):

1. [T02/T04/T06/T08 — canonical body-id enforcement] 0016 permits body_id = NULL with a planet_name audit fallback. Fix: in 0016 — require a valid canonical body id on BOTH claim RPCs (CREATE OR REPLACE claim_home_planet + claim_colony: body_id is a REQUIRED parameter; insert it); ALTER COLUMN body_id SET NOT NULL (after the RPC change; document backfill requirement for any applied stack — write-only stack means no data exists yet, so SET NOT NULL is safe at apply time; add a guarded backfill UPDATE from planet_name via the canonical derivation ONLY IF rows exist — keep it simple and document); REMOVE the planet_name fallback in the audit trigger function (CREATE OR REPLACE: write NEW.body_id, no COALESCE with planet_name). Update 10_home_claim_atomicity.sql to call the RPCs with body_id (and add a missing-body_id case → raises).

2. [T08 — starter-grid contract] onboarding grants {housing:1} but player.ts createPlayer makes an empty grid and both claim RPCs insert housing:0. Fix: ONE shared starter-grid contract — export `STARTER_STRUCTURES = { housing: 1 }` from src/sim/player/grid.ts (allowed: grid.ts — add to allowed list) and use it in onboarding.ts (initialStructuresFor), player.ts (createPlayer structure grid), and the claim RPCs (0016 CREATE OR REPLACE: insert housing 1). Tests assert all three paths produce the identical starter grid.

3. [T03/T04/T07 — unified protection predicate] deriveProtection requires isHome && unconquerable; transferOwnership/conquestTransfer block on unconquerable alone. Fix: centralize — export `assertOwnershipParity(isHome, unconquerable)` (or reuse the ownershipFor validation) from ownership.ts and call it at the top of protection.deriveProtection, ownership.transferOwnership, and transfer.conquestTransfer so an unequal-flag record is REJECTED (throw descriptive Error) before ANY protection/transfer decision. All three then use the identical predicate (protected ⇔ isHome && unconquerable). Negative tests in each module.

4. [T01/T08 — boundary module placement] id.ts with crypto.randomUUID/Date.now/Math.random must NOT live inside src/sim/player/**. Fix: MOVE generatePlayerId to NEW src/boundary/id.ts (JSDoc @boundary: nondeterministic id generation allowed by design, outside the simulation layer). Delete src/sim/player/id.ts. Update ALL importers (player.ts no longer re-exports it; save.ts/UI call sites import from '../../boundary/id' — verify each; the UI changes are compile-required). Tests: no module under src/sim/** imports boundary/id (add a small purity test asserting the import boundary if not already covered by sim-purity.test.ts — you may extend tests/sim-purity.test.ts: allowed).

5. [T04/T07 — SQL conquest acquisition method] The 0005 conquest update does not set acquisition_method='conquest'. Fix in 0016: CREATE OR REPLACE the conquest/attack RPC update (read 0005 for the exact function + statement) to set acquisition_method = 'conquest' when owner_id changes; the audit trigger already copies NEW.acquisition_method (verify the OR REPLACE from round 1 kept this) so event method and row method agree. Add a contract-test assertion in 10_home_claim_atomicity.sql (or a new section) that a conquest-style owner update persists acquisition_method='conquest' AND writes an audit row with method 'conquest'.

VERIFY (focused per-file runs only): `npx tsc -b` exit 0; run the 7 player test files + tests/player-save.test.ts + tests/sim-purity.test.ts — all pass (report per-file counts); SQL static read-back only.

REPORT: per-finding changed lines + which test covers which finding.
