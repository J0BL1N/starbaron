TASK (StarBaron PHASE 7 whole-phase audit FAIL — FIX FINDINGS 1-4 (finding 5 is a docs-reference correction handled separately), no broadening; this closes Phase 7):

ALLOWED FILES: src/sim/combat/attack-orders.ts, src/sim/combat/invasion.ts, src/sim/combat/resolution.ts, src/sim/combat/sim-harness.ts, src/sim/ui/attack-notifications.ts, tests/attack-orders.test.ts, tests/invasion.test.ts, tests/resolution.test.ts, tests/sim-harness.test.ts, tests/attack-notifications.test.ts. Do NOT touch anything else (home-immunity.ts, capture.ts, combat-reports.ts, conquest-cost.ts, casualties.ts, defense.ts stay as-is).

RESTRICTIONS: purity preserved (no nondeterministic APIs, no module-level MUTABLE state — tables deep-frozen, no time-source reads, no environment-dependent formatting APIs); no `any`; strict TS; deterministic.

BANNED COMMENT TOKENS (do NOT write these literals in comments, in any case): any, Math.random, Date.now, performance.now, localeCompare, locale, wall, clock, scene, Three.js, global state, shared mutable data, random.

FINDINGS (fix exactly these; current staging state):

1. [T01/T03/T08 — guards not wired into the chain] attack-orders.ts does NOT call guardLaunch (no target-owner/immunity input at all); resolution.ts does NOT call assertConquestPermitted. A protected home can be launched against and resolved as an attacker victory before capture's downstream transfer refusal. Fix: (a) launchAttack gains a `targetOwner: PlayerState | null` input (the real target owner — READ home-immunity.ts's guardLaunch/HomeImmunityInput signature) and calls `guardLaunch({ targetId, ownerPlayer: targetOwner, attemptedAt: launchAt })` — `allowed === false` → throw the home-world Error (the guard's semantics); (b) resolveBattle (or its caller boundary) gains the same targetOwner input + resolvedAt and calls `assertConquestPermitted` — throws on home; (c) update tests: launching on a home world throws; resolving a battle on a home world throws. NOTE: do NOT change home-immunity.ts — import its guards (they're the locked T08 surface).

2. [T10 — harness not the full chain] sim-harness.ts imports only attackLaunchCost/resolveBattle/applyCasualties/conquestCostFor; it never calls launchAttack, capturePlanet, or buildCombatReport; substitutes `captured = outcome.result === 'victory'`; generates non-canonical ids (`target:<scenarioId>`) capturePlanet would reject. Fix: make runScenario model the REAL chain: (a) canonical target fixture — the scenario input gains a `target: { bodyId: string (canonical — use a parseCanonicalId-valid id like the tests' fixtures: READ tests/capture.test.ts's TARGET/TARGET_ID convention and use the same shape), name: string, tier: number, ownerId: string }`; (b) call launchAttack (with the target owner + guard — it now throws on home; scenario targets are attackable) → AttackOrder; (c) resolveBattle (with targetOwner) → outcome; (d) applyCasualties → ledger; (e) victory → conquestCostFor + capturePlanet (with the REAL ownership record for the target — build it via the ownership module's ownershipFor like capture.test.ts does, NOT fabricated flags; the target is a normal colony — isHome false/unconquerable false via ownershipFor with the real defender id) → captured = result.outcome === 'captured'; (f) buildCombatReport → the report in ScenarioResult (add `report: CombatReport`); replayCheck compares the new leaves too; travelSeconds/launchCost stay. Update the tests: the full chain produces a captured planet via the real transfer; non-canonical target ids rejected.

3. [T09/T11 — notifications not bound to the report] attack-notifications.ts projects result notifications from independently supplied { result, attackerWon } — redefining winner semantics (attackerWon actually means defender won). Fix: battleResultNotification accepts `report: CombatReport` (T09's validated winner/loser) instead of { result, attackerWon }; derive the defender-perspective message from report.result + report.sections.winner (winner === defenderId → 'you repelled'; winner === attackerId → 'fell to the attackers'; stalemate → 'attackers withdrew'); keep the linked orderId (the caller passes it — READ combat-reports.ts's CombatReport shape: it has orderId? if not, add orderId to the notification input separately); update tests (message derivation from a constructed report, perspective flip via winner identity).

4. [T02/T01 — troops exceed fleet capacity] invasion.ts permits a force whose recruited troops exceed its supplied fleetSize (incl. fleetSize: 0) — cannot be carried into launchAttack (troopsCommitted <= fleetSize). Fix: constrain recruitment/commitment to fleet capacity: recruitTroops REJECTS `desiredTroops > fleetSize` (RangeError 'cannot recruit N troops into a fleet of M') — the fleet carries the committed force; update tests (desired > fleetSize throws; desired == fleetSize OK; the existing fleetSize: 0 + desired 0 edge OK).

VERIFY (focused per-file runs only): `npx tsc -b` exit 0; run ALL 5 test files with --pool threads — all pass (report per-file counts). DO NOT run the full suite.

REPORT: per-finding changed lines + which test covers which finding.
