TASK (StarBaron PHASE 3 whole-phase audit FAIL — FIX ALL FINDINGS, no broadening; this closes Phase 3):

ALLOWED FILES (ONLY): src/sim/core/alloys.ts, src/sim/structures/framework.ts, src/sim/structures/queues.ts, src/sim/structures/housing.ts, src/sim/structures/production.ts, src/sim/core/offline-model.ts, src/sim/balance/harness.ts, src/sim/planets/quirk-model.ts, and their test files (tests/alloys.test.ts, framework.test.ts, queues.test.ts, housing.test.ts, production.test.ts, offline-model.test.ts, harness.test.ts, quirk-model.test.ts). Do NOT touch anything else (accrual.ts/economy.ts/effects.ts stay LOCKED and unmodified — new code delegates to them).

RESTRICTIONS: purity preserved (no nondeterministic APIs, no module-level MUTABLE state, no wall-clock, no locale APIs, no banned comment tokens — the literal tokens flagged before: 'any', 'Math.random', 'Date.now', 'localeCompare', 'global state', 'shared mutable data'); no `any`; strict TS; deterministic. DESIGN's locked rule: structure levels are UNLIMITED, no numeric prerequisites — remove the invented constraints.

FINDINGS (fix exactly these; reference files at current staging state):

1. [T01/T02 — alloys.ts] Alloy ledger has no walletInvariants export (credits has it). Add the IDENTICAL invariant function (same signature/ordering: credits finite >= 0, alloys finite >= 0) to alloys.ts — either duplicate the implementation or extract one shared helper in a NEW src/sim/core/ledger.ts (preferred: single implementation both modules import; allowed file). Tests: walletInvariants parity (same tamper classes as transactions tests).

2. [T04/T07/T10 — framework.ts] REMOVE the invented numeric PREREQUISITES table and the hard MAX_LEVEL = 100: DESIGN locks unlimited structure levels and no numeric prerequisites. Delete PREREQUISITES/Prerequisite/PREREQUISITE_SOURCE/maxLevelFor/MAX_LEVEL and every use (prerequisitesMet, canBuild's prerequisites reason, validateGrid's max check, structureSummary's prerequisites field — remove the field or make it always []; adjust tests). canBuild's ladder becomes: unknown-structure → insufficient-credits → insufficient-alloys (no prerequisites rung). validateGrid: levels are finite non-negative integers only (no max check). UPDATE framework tests (remove prereq/max tests; keep cost/ladder/grid tests).

3. [T04/T07 — queues.ts] Centralize the level rule: after finding 2, the rule is finite non-negative integers with NO hard cap. queueConstruction already validates integers >= 0 (keep); remove any maxLevelFor-dependent checks; add a consistency assertion in queueInvariants: every job's levels are finite non-negative integers and toLevel === fromLevel + 1 (already present — keep). Update queues tests that referenced maxLevelFor/100.

4. [T03/T08/T10 — offline-model.ts + harness.ts population] Both use the legacy raw-cap population model, omitting tier cap multipliers, diminishing effective levels, and quirk growth/cap modifiers (concrete failure: a tier-2 planet at 5,500 pop is capped to 5,000 by offline progress → negative growth delta, while live accrual allows growth to 6,000). Fix: EXTEND src/sim/core/population-model.ts (allowed) with a derived-projection entry point: `projectedPopulation(input: { population: number; derived: { populationCap: number; populationPerSec: number } }): { population: number; growthDelta: number }` — clamped growth over a seconds window using the ALREADY-DERIVED cap/rate (the caller obtains them from the locked accrual.ts computePlanetDerived — the projection itself never re-derives). offline-model.ts and harness.ts route their population growth through projectedPopulation (harness: per tick; offline-model: banked window), feeding cap/rate from computePlanetDerived for the planet's tier/grid/quirks. Tests: the tier-2-5500 scenario from the finding (offline + harness must NOT produce a negative growth delta; growth proceeds toward the derived cap).

5. [T06 — production.ts] `localeCompare` at line ~206 violates locale-independent determinism. Replace with canonical STRUCTURE_IDS index ordering (or omit sorting — the mapping already follows the fixed roster order); update the sorting test to assert the roster order explicitly.

6. [T05 — housing.ts] housingGrowthBonus re-derives HOUSING_GROWTH_PER_LEVEL × level instead of wrapping the locked formula. Delegate: `housingGrowthBonus(level) = populationGrowthPerSec(level, 0) - BASE_GROWTH_PER_SEC` (VERIFY population.ts's signature; mirror exactly). Update tests to pin the delegation (same numbers, but the source is the locked function).

7. [T09 — quirk-model.ts] The module-level lookup maps are mutable. Wrap them in Object.freeze (deep-freeze if they hold objects) with readonly typing preserved. Add a test asserting Object.isFrozen on both.

8. [T06/T10 — harness.ts income] Harness uses productionSummaryFor(...).total which EXCLUDES baseline passive income (live accrual includes baselineIncomePerSec + binarySystem's level-0 floor). A tier-1 empty grid earns 10 cr/s live but 0 cr/s in the harness. Fix: harness income per tick = the LOCKED derived credits rate (computePlanetDerived for the current grid/tier/quirk-free planet) — add baseline exactly once and include the binary floor semantics if the derived function handles it; document the source. Update harness tests (empty-grid tier-1 income now 10 cr/s; the standard-run numbers shift — recompute the pinned assertions from the derived function).

VERIFY (focused per-file runs only): `npx tsc -b` exit 0; run ALL 8 test files with --pool threads — all pass (report per-file counts; the suite should now be ~330+ tests). DO NOT run the full suite.

REPORT: per-finding changed lines + which test covers which finding.
