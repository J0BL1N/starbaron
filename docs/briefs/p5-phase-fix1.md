TASK (StarBaron PHASE 5 whole-phase audit FAIL — FIX ALL 7 FINDINGS, no broadening; this closes Phase 5):

ALLOWED FILES (ONLY): src/sim/fleet/*.ts (ships, shipyard, fleet, movement, positioning, render-state, orders, routes, persistence), src/sim/ui/fleet-panel.ts, tests/ships.test.ts, tests/shipyard.test.ts, tests/fleet.test.ts, tests/movement.test.ts, tests/positioning.test.ts, tests/render-state.test.ts, tests/orders.test.ts, tests/routes.test.ts, tests/persistence.test.ts, tests/fleet-panel.test.ts, supabase/migrations/0017_fleets.sql, supabase/tests/11_fleets.sql. Do NOT touch anything else (locked modules unmodified; ui/validate.ts stays).

RESTRICTIONS: purity preserved (no nondeterministic APIs, no module-level MUTABLE state — tables deep-frozen, no time-source reads, no environment-dependent formatting APIs, no rendering-library imports); no `any`; strict TS; deterministic.

BANNED COMMENT TOKENS (do NOT write these literals in comments, in any case): any, Math.random, Date.now, performance.now, localeCompare, locale, wall, clock, scene, Three.js, global state, shared mutable data, random. (Reword existing occurrences — "no wall clock" → "no time-source reads"; "any" → "strictly typed".)

FINDINGS (fix exactly these; current staging state):

1. [ALL T05 modules] Banned comment tokens throughout: ships.ts:19, shipyard.ts:17, fleet.ts:32, orders.ts:27, render-state.test.ts:280 (+ grep the whole src/sim/fleet/ + tests for the banned list). Reword every occurrence in CODE and TESTS.

2. [T02-T05, T09 — shared validator] createFleet (fleet.ts:188), canBuildShips (shipyard.ts:160), movement departure (movement.ts:143), route planning (routes.ts:161) duplicate local positive-timestamp checks; isArrived (movement.ts:232) accepts zero/negative at. Fix: import assertPositiveAt from ../ui/validate and use it for ALL timestamp inputs across src/sim/fleet/ + src/sim/ui/fleet-panel.ts; DELETE the local copies; isArrived must reject zero/negative at too (RangeError). Keep field-specific validators for non-timestamp quantities only.

3. [T07 — order lifecycle timestamps] activateNext/completeOrder/cancelOrder only validate at > 0 — activation before issuedAt, activation after expiresAt, completion/cancellation before issuance all pass. Fix: enforce ONE boundary contract (exported consts ORDER_MIN_AT semantics): at >= issuedAt for ALL transitions (RangeError otherwise); expiry policy: activating an order with expiresAt !== null AND at >= expiresAt → the order is EXPIRED (throw RangeError — document: expired orders cannot activate; cancellation of an expired-but-issued order is allowed? — NO, keep it simple: any transition on an expired order throws with 'expired' message; document). Add boundary tests: activation at exactly issuedAt (ok), just before (throws); activation at/after expiresAt (throws); complete/cancel before issuedAt (throws).

4. [T10 — persistence route geometry] Snapshot route validation doesn't connect legs to their waypoints or recompute geometry/timing — a route whose waypoints say A→B but whose leg travels A→C passes if totals agree. Fix: in snapshotInvariants + the deserialize deep pass, for each route: (a) leg[i].from ref must equal waypoint[i].ref and leg[i].to ref must equal waypoint[i+1].ref; (b) leg[i].distancePc must equal distanceBetween(waypoint[i].position, waypoint[i+1].position) (movement.distanceBetween) within an epsilon (1e-9 relative — document); (c) leg[i].arrivalAt must equal movement.arrivalTime(leg[i].departureAt, distance, speed) recomputed. Add corrupted-leg/waypoint regression tests (A→C leg with A→B waypoints fails).

5. [T10 — SQL mirror] 0017_fleets.sql doesn't mirror the full TS graph: no route legs/totalDurationSec, no sim createdAt, no activeOrderId/one-active-order representation, JSONB shapes unchecked. Fix (write-only): extend fleet_route with legs jsonb not null + total_duration_sec numeric not null check >= 0; fleet row: add sim_created_at timestamptz not null; fleet_order: add active boolean not null default false + a partial UNIQUE index (fleet_id) where active (enforces one-active-order in SQL); add CHECK constraints for the JSONB shapes where practical (composition: jsonb_typeof = 'object'; location/target/waypoints: jsonb_typeof = 'object'/'array' respectively) — document that deep shape validation stays in TS (the SQL enforces structural types only). Extend 11_fleets.sql with: shape-CHECK probes (bad jsonb_typeof insert fails), the partial-unique-index probe (second active order for the same fleet fails), sim_created_at presence, route legs/total_duration_sec presence. Do NOT apply.

6. [T03/T06/T07/T10 — frozen tables] Module-level lookup arrays/records are readonly-typed but not runtime-frozen: render-state.ts:65 + fleet/orders/persistence lookup tables. Fix: deep-freeze them (Object.freeze on the container AND contained objects/arrays); extend the purity tests (Object.isFrozen / isDeepFrozen assertions, source-scan pattern where the repo uses it).

7. [T02 — shipyard construction queue] shipyard.ts has a single build outcome/completion timestamp but no construction QUEUE model (the roadmap's 'queue integration'). Fix: add to shipyard.ts an immutable construction queue: `ShipyardQueue = { shipyardLevel: number; jobs: ShipBuildJob[] }` where `ShipBuildJob = { id: string (fnv1a(class|count|at|index)); shipClass: ShipClassId; count: number; startedAt: number; completesAt: number; status: 'building' | 'done' }`; pure functions: `enqueueShipBuild(queue, request, fleet): ShipyardQueue` (validates the existing ladder + sequential completion: jobs complete in FIFO order; fleet accounting: cap checked against CURRENT ships + ALL building jobs' counts), `shipJobsAt(queue, at): ShipBuildJob[]` (due jobs — inclusive boundary), `completeShipJobs(queue, at): { queue: ShipyardQueue; completed: ShipBuildJob[] }` (idempotent, mirroring queues.ts semantics). shipyardStateFor/ canBuildShips stay (canBuildShips becomes the eligibility basis enqueueShipBuild calls). Tests: enqueue ladder, FIFO completion, cap accounting incl. in-flight counts, idempotent completion, id determinism, immutability.

VERIFY (focused per-file runs only): `npx tsc -b` exit 0; run ALL 10 test files with --pool threads — all pass (report per-file counts). DO NOT run the full suite.

REPORT: per-finding changed lines + which test covers which finding.
