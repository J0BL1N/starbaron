TASK (StarBaron PHASE 2 whole-phase audit re-run FAIL — FIX ONLY THESE 2 FINDINGS, no broadening):

ALLOWED FILES: src/sim/player/claim.ts, src/sim/player/player.ts, tests/claim.test.ts, tests/player-save.test.ts (only if a compile/behavior fix requires — verify against the failing tests first), tests/onboarding.test.ts (only if entryFlow behavior changes — verify). Do NOT touch any other file.

RESTRICTIONS: purity preserved; no `any`; strict TS; deterministic. The canonical flow (assignment.selectHomeWorld, onboarding.entryFlow, colonisation.colonise) is AUTHORITATIVE and unmodified — the legacy claim.ts path must DELEGATE to it or be retired. Banned comment tokens: any, Math.random, Date.now, performance.now, localeCompare, locale, wall, clock, scene, Three.js, global state, shared mutable data, random.

CODEX FINDINGS (fix exactly these; current staging state):

1. [claim.ts:36-37,165-166 + player.ts:14-15] claim.ts DIRECTLY imports the planet catalogue (PLANETS) and claimHomePlanet selects from it; createPlayer exposes that bypass. The Phase 2 import boundary requires catalogue eligibility to be CALLER-INJECTED. Fix: remove the PLANETS import from claim.ts; change claimHomePlanet's signature to require an injected eligible set (e.g. `eligible: readonly { bodyId: string; name: string; tier: number }[]` or the exact shape assignment.selectHomeWorld expects — READ assignment.ts's signature and reuse its input type); delegate the selection exclusively to assignment.selectHomeWorld (injected eligible + taken set); createPlayer (player.ts) must pass an injected eligible set from ITS caller (make it a required parameter — check its call sites: tests + src/ui/useGameState.ts — READ them; update call sites to supply the catalogue-based eligible set; src/ui/useGameState.ts is OUT of the allowed list — if it must change, ADD it to the allowed set and note it in the report). No `PLANETS` import remains in player/**.

2. [claim.ts:145-157] The legacy exported `colonise` path derives existingOwners from ONE PlayerState and ignores every canonical colonise rejection except 'already-owned' — it can create a colony despite insufficient-funds/protected/missing global ownership, and two player states can claim the same body. Fix: either (a) REMOVE the legacy colonise mutation from claim.ts's public API (retire it; update tests that use it to use the canonical colonisation.colonise with a global ownership overlay), or (b) require caller-supplied GLOBAL canonical ownership/protection/requirements inputs and propagate EVERY ColonisationResult rejection before constructing OwnedPlanet. PREFER (b) if the legacy call sites (tests + useGameState) depend on its signature — but the cleanest is (a) if the canonical path covers the behavior; document the choice. The no-duplicate-claims invariant must hold: two player states can NEVER claim the same body through any exported path.

VERIFY: `npx tsc -b` exit 0; run tests/claim.test.ts tests/player-save.test.ts tests/onboarding.test.ts tests/assignment.test.ts tests/colonisation.test.ts with --pool threads — all pass (report per-file counts). DO NOT run the full suite.

REPORT: per-finding changed lines + which test covers which finding + any allowed-list additions needed.
