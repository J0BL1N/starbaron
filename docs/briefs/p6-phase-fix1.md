TASK (StarBaron PHASE 6 whole-phase audit FAIL — FIX ALL 6 FINDINGS, no broadening; this closes Phase 6):

ALLOWED FILES (ONLY): src/sim/intel/*.ts (levels, permissions, scouts, missions, reports, staleness, pvp-gate, store, sensors), src/sim/ui/hover-intel.ts, tests/levels.test.ts, tests/permissions.test.ts, tests/scouts.test.ts, tests/missions.test.ts, tests/reports.test.ts, tests/staleness.test.ts, tests/pvp-gate.test.ts, tests/store.test.ts, tests/sensors.test.ts, tests/hover-intel.test.ts, supabase/migrations/0018_intel.sql, supabase/tests/12_intel.sql. Do NOT touch anything else (ui/info.ts, ui/validate.ts, fleet/*, world/* stay unmodified).

RESTRICTIONS: purity preserved (no nondeterministic APIs, no module-level MUTABLE state — tables deep-frozen, no time-source reads, no environment-dependent formatting APIs, no rendering-library imports); no `any`; strict TS; deterministic.

BANNED COMMENT TOKENS (do NOT write these literals in comments, in any case): any, Math.random, Date.now, performance.now, localeCompare, locale, wall, clock, scene, Three.js, global state, shared mutable data, random.

FINDINGS (fix exactly these; current staging state):

1. [T01/T05/T07/T09 — scanned→alliance leak] reports.ts:97 maps `scanned` to the `alliance` info tier; pvp-gate.ts:152 applies it for STRANGERS — a stranger with fresh scanned intel receives ALLIANCE-only fields, contradicting permissions.ts:121 ({public, intel}). Fix: restructure REVEAL_MATRIX as a FIELD-KEY-level reveal policy that NEVER emits alliance-tier fields for the stranger path: define per intel level the exact FIELD KEYS revealed (from the info contract's public + intel field sets ONLY for stranger-visible tiers; 'observed' → public keys; 'scanned' → public + a few intel keys (population/structures summary — pick keys from the contract's intel tier); 'scouted'/'deep recon' → public + intel keys; 'full intelligence' → ALL keys (owner path only — the gate already restricts full-intelligence to owners; document)); update reports tests (the exact per-rung key sets) + pvp-gate tests (stranger with scanned intel receives NO alliance-tier fields — add a regression).

2. [T02/T06/T07/T08 — decay compounds] staleness.ts:153 writes the DECAYED level back with the original timestamp; running the decay job again subtracts rungs AGAIN; pvp-gate computes one decay from the record — the same report shows different levels depending on whether storeApplyDecay ran. Fix: RETAIN the raw/promoted level in storage; decay is a READ PROJECTION ONLY: (a) staleness.ts: applyDecay must NOT overwrite the level — change its contract to only remove expired records (rename/repurpose: `applyDecay` → returns the store with expired dropped, level UNCHANGED — or split: keep `decayedLevel` as the read projection; `applyDecay` (store-level) removes expired only); (b) store.ts storeApplyDecay uses the new semantics; (c) pvp-gate/hover-intel compute decayedLevel at READ time (they already do); (d) update the tests that pinned the overwrite behavior (level preserved after storeApplyDecay; decayedLevel still projects correctly).

3. [T03/T04 — mission intel cap not enforced] scouts.ts caps scout maxIntelLevel, but missions.ts recordMissionIntel accepts EVERY IntelLevel incl. 'full intelligence' from a 1-scout mission. Fix: persist the launch-derived maximum on ScoutMission (add `maxIntelLevel: IntelLevel` field — set at launchScoutMission from scouts.maxIntelLevelForScouts/composition); recordMissionIntel REJECTS `gained` above mission.maxIntelLevel (descriptive Error). Update missions tests (1-scout mission recording 'scanned' ok, 'full intelligence' throws; multi-scout caps).

4. [T08 — store/SQL timestamp divergence] store.ts allows lastUpdatedAt null (never-updated state); 0018_intel.sql makes last_updated_at NOT NULL — the SQL mirror can't persist a valid record. Fix: make the SQL column NULLABLE with `check (last_updated_at is null or last_updated_at > 0)` (mirror the TS never-updated state); update 12_intel.sql probes accordingly (a null last_updated_at insert succeeds; a 0/negative insert fails).

5. [T07/T08 — no server-side projection surface] The RPC boundary is only described in 0018 comments; RLS protects rows but nothing enforces the viewer×target projection in code. Fix: implement the server-side boundary as a PURE module `src/sim/intel/server-gate.ts` (NEW allowed file) + tests/server-gate.test.ts: `serverViewFor(input: { viewer: ViewerContext; target: TargetContext; store: IntelStore; contractFields: readonly InfoField[]; values: ReadonlyMap<string, string | number | null>; at: number }): GatedView` — reads ONLY the viewer's own store (storeQuery with the viewer id — the record must belong to the viewer; a target the viewer has no record for + stranger → blocked 'no-intel'), applies pvpGatedView, returns the gated view; document that this is the exact function the Supabase RPC wraps (the RPC reads auth.uid() → the viewer's store rows → serverViewFor). ~16-20 tests (viewer-store binding, no-intel, owner, expiry, determinism).

6. [T01/T02 — duplicated validators] levels.ts:103-121 + permissions.ts:102-110 locally reimplement assertNonEmptyString/assertPositiveAt while ui/validate.ts has the canonical pure helpers. Fix: IMPORT the shared validators from '../ui/validate' (it is a pure module with no runtime UI dependency) and DELETE the local copies (keep the local frozen INTEL_LEVELS list — that's a domain table, not a validator). Update tests if they asserted local messages (keep the shared messages identical).

VERIFY (focused per-file runs only): `npx tsc -b` exit 0; run ALL 11 test files (10 intel/ui + server-gate) with --pool threads — all pass (report per-file counts). DO NOT run the full suite.

REPORT: per-finding changed lines + which test covers which finding.
