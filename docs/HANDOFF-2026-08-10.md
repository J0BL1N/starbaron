# StarBaron Handoff — 2026-08-10

> Written when Jay switched to TradieHubAU mid-session. Resume point + current state.

## Where things stand

- **Branch:** `staging` · **HEAD:** `123fac4` (2.5D planetgen core foundation — committed)
- **Working tree:** 23 uncommitted files = the **star-first 3D planetgen work** (Kimi wrote it, stalled on jsdom test noise, got killed) **+ today's visual fixes** (below)
- **Dev server:** was running on `localhost:5173` (background proc `proc_b4679c890c06`), preview opened in Hermes pane. May or may not still be alive — check before resuming.

## Visual bugs found (Jay: "looks really buggy") + fixes applied in `src/ui/planetgen3d/render.ts`

All edits below are **APPLIED but NOT yet verified** (tsc last passed at `123fac4` before edits; HMR will pick them up):

1. **Asteroids were blocky uniform squares** (6-seg sphere, single colour) → `SphereGeometry(1,10,10)`, per-instance colours (6-tone rock palette via `ASTEROID_COLORS`), white base material + `setColorAt`, plus per-instance pseudo-random scale + rotation in the animate loop (index-hash, still deterministic — no `Math.random`).
2. **Star blown out** → glow sprite alpha toned down (0.9→0.5 core, 0.35→0.16 mid) and glow scale halved (`*26 → *13` system view, `*10 → *5.5` planet view; both pulse sites updated).
3. **Square backdrop dots** (galaxy backdrop + universe `PointsMaterial`) → added `map: makeCircleSprite(), alphaTest: 0.1, depthWrite: false` to both.
4. **Faint orbit tracks** → `0x2a3152` → `0x5a6ca0`, opacity 0.18/0.4 → 0.4/0.65, `AdditiveBlending`, `depthWrite: false`.

## Still pending

- **CSS:** `.colonise-row` overlap (COLONISE button vs "Expand your empire" text in narrow panes) — add `flex-wrap: wrap` + `min-width: 0` on `.colonise-name` (src/ui/App.css ~line 278). NOT applied yet.
- **Verification gate (uncommitted work can't merge without it):**
  1. `npx tsc -b` (re-run — edits unverified)
  2. `npm test` — full suite ~634; the jsdom canvas shim (`tests/setup-canvas.ts` + `vitest.config.ts`) was the mid-fix blocker when the Aug-9 session died. **WARN: previous `npx vitest run tests/planetview.test.tsx` fork-bombed (78 node procs, 24GB RAM) when its shell died — run with `timeout` and kill cleanly.**
  3. `npm run build`
  4. Codex read-only audit → commit to staging (explicit `git add` paths, no `-A`) → ROADMAP update (P3-T05-C/D still Not started)
- **Jay authorisation still owed:** migration `0012` (fortification/build_structure) apply to live Supabase `ogsleukfykumxsyvyusz`.

## Notes on visuals (for next look)

- Planets = proper shaded spheres ✅ · rings exist but thin (planet ring shader, not orbit tracks) · host-star dots already round (circle sprite) · locators static, fleetLayer reserved.
- Known skill pitfalls that apply: THREE.Points needs circle sprite; Codex can't see pixels — visual verify in browser/preview.
